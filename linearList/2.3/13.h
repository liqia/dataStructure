#include <iostream>
#include "LNode.h"
using namespace std;
LNode *conLink(LNode *a,LNode *b){
    LNode *head;
    while(a!=NULL&&b!=NULL){
        if(a->data<b->data){
            head=a;
        }
    }
}
